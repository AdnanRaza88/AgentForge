# AgentForge Harness (v2)

Typed loop state machine, circuit breakers, snapshot/revert, telemetry JSONL.

See `packages/core/src/harness/`.

Phases: IDLE → PERCEIVE → PLAN → ACT → PERMISSION → EXECUTE → VERIFY → REFLECT → REPORT → PERSIST.
