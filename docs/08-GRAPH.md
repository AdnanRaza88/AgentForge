# TaskGraph (v2)

DAG of work nodes with parallel ready-set scheduling and path-overlap serialization.

See `packages/core/src/graph/`.

Node kinds: tool_call, subagent, verification, checkpoint, human_review, plan.
